[project]
name = "m2-ragbot"
version = "1.0.0"
description = "HDFC mutual fund facts-only RAG FAQ assistant"
requires-python = ">=3.11"

[tool.vercel]
# FastAPI ASGI entry — bootstrap + Phase 5 API/UI (used by Vercel Python deploy)
entrypoint = "phases.phase6.api_server:app"
